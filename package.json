{
  "name": "zingage-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "db:ddl:init": "psql \"$DATABASE_URL\" -f sql/ddl_init.sql",
    "db:ddl:norm": "psql \"$DATABASE_URL\" -f sql/ddl_norm.sql",
    "etl": "tsx src/etl/transform.ts",
    "db:dml": "psql \"$DATABASE_URL\" -f sql/dml.sql",
    "queries": "psql \"$DATABASE_URL\" -f sql/queries.sql",
    "all": "npm run db:ddl:init && npm run db:ddl:norm && npm run etl && npm run db:dml",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "csv-parse": "^6.1.0",
    "dotenv": "^17.2.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.2"
  }
}
